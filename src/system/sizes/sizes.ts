import spacing from "../spacing/spacing";
import { Sizes } from "./sizes.types";

const sizes: Sizes = {
  ...spacing,
  max: "max-content",
  min: "min-content",
  full: "100%",
};

export default sizes;
