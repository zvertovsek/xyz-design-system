import { BasicToggle, IconToggle } from "./basicToggle";
import { PrimaryToggle } from "./primaryToggle";
import { SwitchToggle } from "./switchToggle";
import * as ToggleTypes from "./toggle.types";

export { BasicToggle, IconToggle, PrimaryToggle, SwitchToggle, ToggleTypes };

export default {
  BasicToggle,
  IconToggle,
  PrimaryToggle,
  SwitchToggle,
  ...ToggleTypes,
};
