export interface SliderViewModel {
  /**
   * Label name to show before the slider.
   */
  startLabel?: string;
  /**
   * Label name to show after the slider.
   */
  endLabel?: string;
  /**
   * Flaf that indicates whether the slider should be reversed or not.
   */
  isReverse?: boolean;
}
